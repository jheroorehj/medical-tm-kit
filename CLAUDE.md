# medical-tm-kit — 작업 규칙

바이오헬스AI 해커톤 산출물입니다. 이 파일은 **이 저장소에서 작업할 때 반드시 지킬 규칙**만
담습니다. 실행 절차·데모 시나리오·기획서 양식은 [`HACKATHON.md`](HACKATHON.md) 에 있습니다.

> **문서 우선순위**: 공식 배점표 > 대회 안내 사항 > 강의 정리(`docs/바이오헬스AI_해커톤정리.md`).
> 충돌하면 위쪽을 따릅니다. 강의는 Teachable Machine 전제로 쓰여 있으니, TM 관련 서술은
> 이제 참고 자료로만 읽으세요 (5장 참조).

## 프로젝트 방향 (2026-07-29 갱신)

| | |
|---|---|
| **도메인** | 망막(안저) 이미지 판독 보조 |
| **라벨 구조** | **환자 단위 다중 라벨** (한 환자에 소견이 동시에 여러 개) |
| **모델** | **TM 비의존.** 팀원이 의료 도메인 이미지 분류 모델을 탐색 중 |
| **우리 역할** | **TM 결합 제거 + 모델 백엔드 확장성 설계** |
| 데이터셋 | 미확정 (3장 기준으로 검증할 것) |

**이 두 가지가 현재 코드의 전제를 깨뜨립니다.** 착수 전에 4장(다중 라벨)과
5장(백엔드 확장성)을 반드시 읽으세요. 모르고 손대면 지표가 조용히 틀립니다.

### 팀원에게

이 파일은 **git 으로 공유되고 배포에는 포함되지 않습니다.**
`.vercelignore` 의 `*.md` 와 `vercel.json` 의 `outputDirectory: "web"` 두 겹으로 제외됩니다.
둘 중 하나를 지워도 다른 하나가 막지만, 둘 다 지우지 마세요.

**Claude Code 로 이 폴더에서 작업하면 이 파일이 자동으로 읽힙니다.** 따로 붙여넣지 않아도
아래 규칙이 적용됩니다. 사람이 읽어도 되는 문서이니 작업 시작 전에 한 번 훑어 주세요.
특히 **1장(작업 순서)** 과 **2장(절대 깨뜨리지 말 것)** 은 어기면 감점으로 직결됩니다.

처음 클론했다면:

```bash
./serve.sh                                  # http://localhost:8000, 앱의 '도움말' 탭부터
node test-logic.mjs                         # 70/70 통과해야 정상
cd prep && pip install -r requirements.txt  # 데이터 작업 담당만
```

---

## 1. 배점이 작업 순서를 정합니다 (100점)

| 배점 | 항목 | 이 저장소에서 뜻하는 것 |
|---:|---|---|
| **15** | 2.1 웹서비스 동작 | 실제로 정확히 동작할 것 |
| **15** | 2.3 모델 연동 | **실제 모델**로 예측이 표시될 것 |
| 10 | 1.1 데이터 이해·전처리 | `prep/` 파이프라인 |
| 10 | 1.2 학습 파이프라인 | **학습 반복과 튜닝 근거** (`HACKATHON.md` 7-A 로그) |
| 10 | 1.3 예측 성능 | 홀드아웃 수치 |
| 10 | 2.2 UI/UX | **최종 사용자(임상의) 관점** |
| 10 | 2.4 결과 시각화 | 신뢰도 표시 포함 |
| 5+5 | 3.1 발표 · 3.2 Q&A | 문제 → 해결 → 결과 |
| 5+5 | 4.1 역할분담 · 4.2 소통 | 협업 흔적 |

### 규칙 1-1. 동작이 최우선

**2.1 + 2.3 = 30점이 "실제로 동작"을 요구합니다.** 기능 추가·리팩터·디자인 개선은
전체 경로(모델 로드 → 판독 → 소견 표시 → 히트맵 → 홀드아웃 평가)가 실제 모델로
한 번 완주한 뒤에 합니다. 완주 전에는 다른 작업을 제안하지 마세요.

확장성 설계도 이 규칙 아래입니다. **백엔드 추상화를 아무리 잘 만들어도 실제 모델
하나가 붙어 동작하지 않으면 30점이 0입니다.** 어댑터를 여러 개 만들기 전에
팀이 고른 모델 하나로 완주하세요.

### 규칙 1-2. 정확도를 숨기지 마세요

배점 1.3 이 *"프로젝트 목적에 맞는 예측 정확도 달성"* 을 직접 평가합니다.
**"정확도는 말하지 않겠습니다" 같은 문구를 산출물이나 발표 자료에 넣지 마세요.**
(이 저장소 초기 문서에 그런 문장이 있었고 정정했습니다.)

올바른 형태: 홀드아웃 수치를 **함께** 제시하고 측정 방법(환자 단위 분리)을 밝힙니다.
다중 라벨에서는 **라벨별 민감도·특이도 + 매크로 평균**이 정확도보다 정보량이 많습니다
(4-D 참조).

### 규칙 1-3. 첫 화면은 임상의 기준으로

배점 2.2 는 최종 사용자 관점을 평가합니다. 판독 화면 첫 인상에 남기는 것은
**검출된 소견 · 유사도 · 권고 · 안전문구 · 데이터셋 출처**뿐입니다.
엔트로피 · ECE · 매크로 평균 같은 용어는 `자세히 보기` 뒤에 둡니다
(`ui.showDetailByDefault: false`). 새 전문 지표를 추가할 때도 같은 규칙을 적용하세요.

### 규칙 1-4. 보이는 것은 동작해야 합니다

배점 2.1(정확히 동작) · 2.4(완성도)에 걸립니다.
**클릭 핸들러 없는 버튼, 값이 채워지지 않는 자리, 빈 카드를 남기지 마세요.**
실제로 `#detail-toggle` 이 배선 없이 화면에 있던 사고가 한 번 있었습니다.
UI 요소를 추가하면 배선과 빈 상태(empty state)를 같이 만드세요.

---

## 2. 절대 깨뜨리지 말 것 (대회 필수 표기)

- **"확률" 금지, "유사도" 사용.** 코드·화면·문서 전부. `probability` 는 내부 변수명만.
- **불확실할 때 단정하지 않습니다.** 임계값 미달·불확실 구간은 `보류` 로 표시하고
  사람 확인을 요청합니다. (다중 라벨에서는 라벨별 불확실 구간 — 4-C 참조)
- **안전 문구 상시 노출** — "교육용 프로토타입이며 실제 진단·치료에는 사용할 수 없습니다".
- **데이터셋 출처·라이선스·라벨링·클래스 구성·한계**를 화면에 표기 (`dataset` 블록).
  미기재면 화면과 도움말 환경 점검에서 경고가 뜨도록 되어 있습니다. 그 경고를 없애지 마세요.
- **업로드 실패 시 내장 샘플 자동 로드** (`failUpload()`). 데모가 끊기지 않게 하는 장치입니다.
- **입력은 이미지 업로드.** 웹캠은 `ui.enableWebcam: false` 로 꺼져 있습니다.
  실시간 판독·시간축 안정화는 웹캠 전용이므로 함께 숨겨져 있습니다. 되살리지 마세요.
- **분류 문제만.** 검출·분할은 대회 범위 밖입니다.
  (다중 라벨은 분류입니다. "어디에 있는지" 를 답해야 하면 범위 밖입니다.)
- **홀드아웃은 학습에 쓰지 않습니다.** 환자 단위 분할을 유지하세요.

이 항목들은 산문이 아니라 `test-logic.mjs` 가 검사합니다 (9장 참조).
**라벨 구조를 다중 라벨로 바꾸면 불변식도 함께 갱신해야 합니다** (임계값 항목 등).

---

## 3. 데이터셋 선정 검증 (주제 확정 전 필수)

데이터셋이 아직 확정되지 않았습니다. 후보가 나오면 **아래를 통과하는지 먼저
확인하고, 통과하지 못하면 후보를 바꾸라고 말하세요.**

### 3-A. 분류로 풀리는가

- [ ] **한 장의 이미지 전체를 보고 사람이 소견을 말할 수 있는가?**
      "어디에 있는지", "몇 개인지", "경계가 어디까지인지"를 답해야 하면
      검출·분할이며 **대회 범위 밖**입니다.
- [ ] 라벨이 **다중 라벨**인가 단일 라벨인가? 등급형(0~4)인가?
      각각 처리가 다릅니다 (4장). 후보의 라벨 구조를 **먼저 확인**하세요.
- [ ] 라벨 단위가 **환자**인가 **이미지(눈)** 인가? 환자 단위면 집계 규칙이 필요합니다 (4-B).

### 3-B. 안저 이미지 특유의 확인 사항

- [ ] **입력 해상도로 병변이 살아남는가?** 안저 조기 병변(미세혈관류)은 원본에서 약 10px 이고
      224 로 줄이면 1px 수준으로 소실됩니다. **TM 을 안 쓰게 되었으므로 이 상한이 풀렸습니다**
      — 5-D 를 읽고 입력 크기를 정하세요.
      **확인 방법**: 후보 이미지를 `prep/pipeline.py` 로 돌려 결과물을 눈으로 보세요.
      사람이 못 알아보면 모델도 못 봅니다.
- [ ] **원형 시야(FOV) 밖 검은 여백**이 있는가? `auto_border_crop` 이 처리하지만 확인 필요.
- [ ] 화질 편차가 큰가? (현장 촬영 데이터셋은 초점 불량·저조도가 섞임)
      `품질 게이트` 로 걸러낼 수 있지만, 학습 데이터에서도 제외 기준이 필요합니다.
- [ ] 좌우안(OD/OS) 구분이 파일명·메타데이터에 있는가?

### 3-C. 확보 가능성

- [ ] **가입만으로 즉시 다운로드**되는가? 승인 대기·교육 이수가 필요하면 시간이 부족합니다.
- [ ] 라이선스가 교육·비상업 사용을 허용하는가? **반드시 확인하고 화면에 표기**합니다.
- [ ] **라벨당 양성 사례가 충분한가?** 다중 라벨은 희귀 라벨의 양성 표본이 극단적으로
      적습니다. 양성이 수십 건인 라벨은 지표가 무의미하니 **제외하거나 상위 라벨로 병합**하세요.
- [ ] 라벨 동시 발생(co-occurrence) 분포를 아는가? 특정 조합만 나오면 모델이 그 조합을
      외웁니다.

### 3-D. 설명 가능성

- [ ] **이 데이터의 편향을 구체적으로 말할 수 있는가?** ("단일 병원", "특정 인구 구성",
      "특정 카메라 기종") 말할 수 없으면 Q&A 에서 무너집니다.
- [ ] 라벨링 방식을 아는가? (전문의 합의 / 단독 판독 / 판독문 자동추출)
      안저 등급은 **관찰자 간 불일치가 흔합니다.** 이건 화면 표기 항목입니다.

찾을 곳: Kaggle · Grand Challenge · MedMNIST · Hugging Face Datasets.
안저 특화: APTOS 2019, EyePACS, Messidor, ODIR-5K(다중 라벨·환자 단위), RFMiD.
피할 곳: 승인 대기·교육 이수가 필요한 것.

---

## 4. 환자 단위 다중 라벨 — 현재 코드가 깨지는 지점

**현재 코드는 전부 "단일 라벨 softmax" 전제입니다.** 다중 라벨로 가면 아래를
고쳐야 하고, 고치지 않으면 **화면은 그럴듯한데 지표가 틀립니다.** 가장 위험한 종류입니다.

### 4-A. 깨지는 곳 목록

| 위치 | 현재 전제 | 다중 라벨에서 해야 할 일 |
|---|---|---|
| `infer.js runClassify()` | 앙상블 평균 후 **합=1 로 재정규화** | **재정규화를 없애야 합니다.** sigmoid 출력을 재정규화하면 라벨 독립성이 파괴됩니다. 가장 먼저 고칠 곳 |
| `infer.js decide()` | argmax 1위 + 단일 임계값 + 1·2위 마진 | 라벨별 독립 판정. **마진 관문은 의미가 없어지므로 제거** |
| `metrics.binaryMetrics()` | 예측 라벨 1개 (`topId`) | 라벨별 2×2 → 매크로/마이크로 평균 |
| `metrics.confusionMatrix()` | N×N 한 장 | 라벨별 2×2 를 N개 |
| `calibrate.js` | top-1 확률 vs 정답 일치 | 라벨별 보정 곡선 |
| `ui.renderBars()` | 내림차순 "1위" 강조 | 임계값 초과 라벨을 **"검출된 소견"** 으로 묶어 표시 |
| `holdout.js runBatch()` | `trueId`/`topId` 단일 문자열 | 라벨 **집합**으로 기록 |
| `classes[].kind` | positive / negative / invalid | "정상(no finding)" 이 라벨로 존재하는지 데이터셋 확인 필요 |
| `temporal.js` | top-1 이동평균 | 웹캠 전용이라 지금은 무관. 다중 라벨이면 라벨별 평활 |

### 4-B. 환자 단위 라벨 → 이미지 단위 예측

안저는 환자당 좌우안 2장 이상입니다. **정답이 환자 단위인데 예측이 이미지 단위면
집계 규칙이 반드시 필요합니다.**

- 집계 규칙을 **설정으로 노출하고 화면에 명시**하세요. 후보:
  - `max` — 어느 눈이든 양성이면 환자 양성. 민감도 우선, 위양성 증가. **선별 목적에 적합**
  - `mean` — 평균. 완만하지만 한쪽 눈만 심한 경우를 놓침
  - `both` — 양쪽 다 양성일 때만. 특이도 우선
- **평가는 정답의 단위에서** 합니다. 환자 단위 정답이면 집계 후 환자 단위로 평가하세요.
  이미지 단위로 평가하면 수치가 실제보다 좋게 나옵니다.
- 분할은 이미 환자 단위입니다 (`pipeline.py` 의 `split.group_regex`). 그대로 유효하고,
  다중 라벨에서 **더 중요해집니다** — 같은 환자의 반대쪽 눈이 홀드아웃에 있으면 누출입니다.

### 4-C. 판단 보류의 재정의

- 단일 라벨: `top-1 < 0.70` → 보류
- 다중 라벨: **라벨별 불확실 구간** `τ_low < p < τ_high` → 그 라벨은 보류
- 케이스 수준 트리아지 (화면에 보여줄 결론):
  - 확정 양성 라벨이 있음 → **전문의 판독 의뢰**
  - 전부 확정 음성 → **정상 소견**
  - 보류 라벨이 있음 → **사람 확인 필요**

임계값은 **라벨마다 다를 수 있습니다.** 놓치면 위험한 소견은 낮게, 위양성이 부담인
소견은 높게. 이 선택의 근거를 화면과 발표에서 설명할 수 있어야 합니다 (배점 1.3·3.2).

### 4-D. 지표

- **라벨별** 민감도·특이도·PPV 를 먼저 내고, **매크로 평균**(라벨 동등 가중)을 대표값으로.
  마이크로 평균은 빈도 높은 라벨에 끌려갑니다.
- 다중 라벨 정확도는 정의가 여러 개입니다. 쓰기 전에 어느 것인지 밝히세요:
  - **subset accuracy** — 라벨 집합이 완전히 일치. 매우 엄격, 보통 낮게 나옴
  - **Hamming accuracy** — 라벨 단위 정확도. 음성이 많으면 부풀려짐
  - 발표에는 **라벨별 민감도·특이도 + 매크로 평균**이 가장 정직합니다.
- 라벨별 양성 표본 수를 **항상 함께** 표기하세요. 양성 20건짜리 라벨의 민감도 95% 는
  거의 의미가 없습니다.

---

## 5. 모델 백엔드 확장성 설계 (우리 역할)

TM 은 더 이상 필수가 아닙니다. 우리 역할은 **TM 결합을 떼어내고 모델을 갈아 끼울 수
있게 만드는 것**입니다.

### 5-A. TM 에 묶여 있는 곳

| 위치 | TM 결합 |
|---|---|
| `web/js/models.js` | `tmImage.load(url+'model.json', url+'metadata.json')`, `normalizeModelUrl()` (TM URL 모양 전제) |
| `web/index.html` | CDN 으로 `@teachablemachine/image` 로드 |
| `web/js/infer.js` | `model.predict(source)` 가 `[{className, probability}]` 를 준다고 전제 |
| 전처리 | TM 이 224 정사각 크롭·정규화를 내부에서 강제 |
| `prep/fetch_model.py` | TM 클라우드 URL 에서 3개 파일 수신 |
| 도움말 탭 STEP 03 | TM 학습 절차 안내 |

### 5-B. 어댑터 계약 (설계 방향)

모델마다 다른 것을 **모델이 선언**하게 만들고, 나머지 코드는 그 선언만 봅니다.

```
ModelAdapter.load(config) → Model

Model = {
  labels:     string[]                       // 클래스 id (config 와 일치 검사)
  inputSpec:  { size, normalize, channels }  // 224|384|512, '0..1'|'-1..1'|'imagenet', 3|1
  outputSpec: { activation }                 // 'softmax'(단일) | 'sigmoid'(다중)
  predict(canvas) → { [labelId]: number }
}
```

- **`outputSpec.activation` 이 판정 계층의 분기 기준입니다.** `softmax` 면 현재 `decide()`,
  `sigmoid` 면 라벨별 판정 경로. 이 값 하나로 4장의 분기가 정리됩니다.
- **`inputSpec` 이 전처리를 결정합니다.** `prep/prep.config.yaml` 의 `size` 와
  `web` 의 추론 전처리가 이 값을 함께 따라야 합니다.
- 백엔드 후보: `tm`(현재) · `tfjs-graph` · `tfjs-layers` · `onnx-web` · `http`(원격 엔드포인트)
- **`http` 백엔드를 넣으면 "브라우저 내 처리" 라는 개인정보 문구를 고쳐야 합니다.**
  영상이 서버로 나가기 때문입니다. 안전 문구와 실제 동작이 어긋나면 감점 이상의 문제입니다.

### 5-C. 살아남는 것 / 바뀌는 것

**그대로 재사용** (작업이 낭비되지 않습니다)

- `prep/` 전체 — 전처리·환자 단위 분할·중복 제거. TM 을 벗으면 **전처리를 우리가
  통제하게 되므로 오히려 더 중요해집니다**
- 품질 게이트 (`quality.js`) — 모델과 무관한 신호처리
- 오클루전 히트맵 (`saliency.js`) — `predict()` 만 있으면 어떤 모델에도 동작
- 캘리브레이션·임계값 스윕 — 개념은 그대로, 라벨별로 확장
- UI 셸·인터랙션·도움말·데이터셋 카드·안전문구·이력·CSV

**고쳐야 함**

- `models.js` → 어댑터 레지스트리로 재작성
- `infer.js` → 재정규화 제거, 판정 분기
- `metrics.js` · `calibrate.js` · `holdout.js` → 라벨 집합 기반
- `ui.js` 판정 카드·막대 → "검출된 소견 목록"
- `test-logic.mjs` → 다중 라벨 단정 추가 (단일 라벨 단정은 남겨 두고 분기)

### 5-D. TM 을 벗는 최대 실익: 입력 해상도 상한 해제

TM 은 **224×224 고정**이었습니다. 안저 조기 병변(미세혈관류)은 원본에서 약 10px 이라
224 로 줄면 1px 수준으로 사라집니다 (강의 7-1 이 지적한 문제).

**백엔드를 열면 384·512 입력 모델을 쓸 수 있고, 이것이 조기 병변 검출에 직접 영향을
줍니다.** 그래서 확장성 설계의 1순위는 추상화의 우아함이 아니라
**입력 해상도를 설정 가능하게 만드는 것**입니다.

부수 효과로 챙길 것: 전처리 커스터마이즈가 가능해졌으므로 안저 표준 전처리를 쓸 수
있습니다 — 원형 FOV 크롭, **녹색 채널**(혈관 대비가 가장 높음), CLAHE.
`prep/pipeline.py` 에 CLAHE·여백 제거가 이미 있습니다.

### 5-E. 모델을 받아오는 방법

- **배포·시연에는 모델 파일을 동봉하는 방식이 안정적입니다** (발표장 네트워크 의존 감소).
  `prep/fetch_model.py` 가 TM 용으로 이 일을 합니다. 다른 백엔드에서도 같은 원칙을 유지하세요.
- 모델 파일은 용량이 큽니다. `web/model/` 은 `.gitignore` 되어 있지 않으니
  **커밋 전에 용량을 확인**하세요. 수십 MB 를 저장소에 올리기 전에 상의할 것.
- 재학습·모델 교체 시 **클래스 라벨 목록이 `project.config.js` 와 일치하는지** 검사하는
  기존 장치(`verifyClasses`)를 유지하세요. 다중 라벨에서도 그대로 유효합니다.

---

## 6. 교체 지점은 2개뿐입니다

주제·모델이 바뀌어도 아래 두 파일만 고칩니다. 나머지 코드는 도메인을 모릅니다.

- `web/project.config.js` — 클래스 · 모델 백엔드·URL · 판정 정책 · 데이터셋 · 카피 · UI 정책
- `prep/prep.config.yaml` — 경로 · 그룹 정규식 · 전처리 · 중복제거 · 균형

**추론 전처리와 학습 전처리는 항상 일치해야 합니다.** 현재 `decision.inferRoi` ==
`preprocess.center_crop` 을 `test-logic.mjs` 가 검사합니다 (실제로 0.70 vs 0.85 로
어긋난 적이 있습니다). 백엔드 도입 후에는 **입력 크기도 같은 방식으로 묶으세요.**

---

## 7. UX 설계 원칙 — 행동경제학 · HCI

### 7-A. 목표는 "신뢰 최대화" 가 아니라 "신뢰 보정"

사용자가 믿고 편하게 쓰게 만드는 것이 목표지만, **의료 AI 에서 신뢰를 무조건 높이는
설계는 위험합니다.** 과신은 **자동화 편향(automation bias)** 으로 이어집니다 —
사람이 AI 를 과신해 틀린 답도 그대로 따라가는 현상이고, 여기서는 환자 위험이 됩니다.

목표는 **신뢰 보정(trust calibration)** 입니다. 모델이 맞을 때는 믿게 하고,
불확실할 때는 의심하게 만드는 것. 이것이 대회의 Human-in-the-Loop 요구와도 같은 방향이고,
"편리함·쉬움" 과도 충돌하지 않습니다. **불확실할 때 불확실하다고 말해 주는 도구가
사용자에게 더 편합니다.** 나중에 뒤통수를 맞지 않기 때문입니다.

### 7-B. 적용할 것

| 원리 | 이 앱에서의 적용 |
|---|---|
| **자동화 편향 대응** | 보류·불확실을 숨기지 않고 다른 색·기호로 표시 (구현됨) |
| **앵커링 회피** | 큰 숫자를 먼저 보여주면 판단이 그 값에 고정됩니다. 유사도 옆에 **권고 문구를 같은 무게로** |
| **프레이밍 일관성** | "유사도" 하나로 통일. 같은 색은 항상 같은 의미 |
| **손실 회피** | 놓침이 위양성보다 두렵다는 것이 민감도 우선의 근거입니다. 그 **이유를 화면에 적어** 임계값 선택을 납득시키세요 |
| **인지 부하 감소** | 점진적 공개(구현됨). 첫 화면 요소를 늘리지 마세요 |
| **시스템 상태 가시성** | 모델 로드 상태 · 품질 지표 · 진행률을 항상 보이게 (구현됨) |
| **오류 예방 > 오류 메시지** | 품질 게이트가 나쁜 입력을 미리 막고 코칭합니다 (구현됨) |
| **되돌리기 가능성** | 초기화 · 판정 당시 임계값 기록으로 재현 (구현됨) |
| **예측 가능성** | 같은 입력 → 같은 출력. 임계값을 바꾸면 화면에 남기세요 |

### 7-C. 다중 라벨에서 추가로 지킬 것

- **소견을 한 줄에 다 늘어놓지 마세요.** 라벨이 많으면 인지 부하가 급증합니다.
  **검출된 소견**(임계값 초과)을 위에, 보류를 그 아래, 음성은 접어 두세요.
- **케이스 수준 결론을 하나 제시하세요.** 라벨 8개의 숫자만 보여주면 사용자가 종합해야
  합니다. "전문의 판독 의뢰 / 정상 소견 / 사람 확인 필요" 중 하나를 크게.
- **라벨 간 확률을 비교하게 만들지 마세요.** sigmoid 출력은 서로 독립이라
  0.8 과 0.7 을 "더 가능성 높음" 으로 비교하는 것이 통계적으로 부적절합니다.
  막대를 나란히 놓으면 사용자가 자동으로 비교합니다. 정렬·강조에 주의하세요.

### 7-D. 설명의 역설 — 히트맵을 다룰 때 주의

**그럴듯해 보이는 설명은 오히려 과신을 키운다**는 것이 설명가능 AI 연구의 반복된
결과입니다. 근거 히트맵을 *"모델이 옳다는 증거"* 로 제시하면 역효과입니다.

**올바른 프레이밍**: "모델이 무엇을 보았는지 검토하세요."
히트맵의 가치는 **지름길 학습을 발견하는 것**이지 판정을 정당화하는 것이 아닙니다.
`interpretMap()` 이 "근거의 68%가 주변부에 있습니다" 처럼 **의심하라는 방향**으로
문장을 만드는 이유가 이것입니다. 이 어조를 바꾸지 마세요.

다중 라벨에서는 **어느 라벨에 대한 히트맵인지 반드시 명시**하세요. 라벨별로 근거
영역이 다릅니다.

### 7-E. 하지 말 것

- 신뢰를 **연출**하는 장치 — 가짜 로딩 지연, 불필요한 애니메이션으로 권위 부여,
  "AI 가 정밀 분석 중" 같은 과장 문구
- 확신을 실제보다 높게 보이게 하는 표현 — 반올림으로 100% 표시, 소수점 과다 표기
- 사용자가 끌 수 없는 자동 판정 — 항상 사람이 최종 판단한다는 구조를 유지

---

## 8. 이미 한 번 겪은 실수 (반복 금지)

### CSS · DOM

- `[hidden] { display: none !important; }` 를 지우지 마세요. 작성자 스타일시트의
  `display` 선언이 브라우저 기본값을 덮어써 `hidden` 이 무력화됩니다.
- **SVGElement 에는 `hidden` IDL 프로퍼티가 없습니다.** `el.hidden = true` 는 속성이
  아니라 JS 확장 프로퍼티만 만듭니다. `ui.show()` 처럼 속성을 직접 조작하세요.
- `img.src = ''` 는 문서 URL 로 해석되어 **항상 truthy** 입니다. `removeAttribute('src')`.
- 모델 클래스명은 모델 메타데이터에서 온 **외부 입력**입니다. `innerHTML` 에 넣기 전에
  `ui.esc()` 를 통과시키세요.

### 모션 (Emil Kowalski 기준)

- **`width` 애니메이션 금지.** 채우기 막대는 `transform: scaleX` + `ui.setFill(el, ratio)`.
  CSS 는 `width:100%` + `transform-origin:left` 로 맞춰져 있습니다.
- `innerHTML` 로 막대를 재생성하면 트랜지션이 재생되지 않습니다. 노드를 재사용하고
  `requestAnimationFrame` 에서 값을 넣으세요.
- **키보드로 여는 것(⌘K)에는 애니메이션을 넣지 마세요.** 반복 동작(탭 전환)은 이동 없이
  불투명도만.
- 모든 UI 전환은 **300ms 이내**. `ease-in` 은 쓰지 않습니다. 토큰
  (`--ease-out`, `--t-press/hover/state/fill`)을 쓰세요.
- 누를 수 있는 요소에는 `:active` 눌림(`scale(.94~.99)`)과 `:focus-visible` 링을 반드시.
- `prefers-reduced-motion` 에서 전체를 차단하지 마세요. **이동·확대·무한반복만** 제거하고
  색·불투명도 전환은 남깁니다.

### 데이터 파이프라인

- **중복 제거는 같은 클래스 안에서만.** 의료영상은 정상과 이상이 시각적으로 비슷해서
  클래스를 넘나들며 dHash 를 비교하면 멀쩡한 클래스가 통째로 삭제됩니다.
  **다중 라벨에서는 "같은 클래스" 정의가 달라집니다** — 라벨 집합이 같은 것끼리
  비교하거나, 전체 풀에서 완전 동일(해밍 0)만 제거하도록 고쳐야 합니다.
- YAML 의 정규식은 **작은따옴표**로. `"^(patient\d+)"` 는 YAML 이 거부합니다.
- `holdout/` 은 증강하지 않습니다.

### 지표

- **sigmoid 출력을 재정규화하지 마세요.** `infer.js runClassify()` 가 현재 앙상블 평균
  후 합=1 로 재정규화합니다. 다중 라벨에서는 라벨 독립성이 파괴됩니다 (4-A).

---

## 9. 커밋 전 검증 게이트

아래를 통과하지 못하면 커밋하지 마세요.

```bash
node test-logic.mjs      # 70/70 — 판정 관문·임상 지표·캘리브레이션 + 대회 불변식 11개
for f in prep/*.py; do python3 -m py_compile "$f"; done
```

**2장의 불변식은 산문이 아니라 실행되는 테스트입니다.** 규칙을 어기면 테스트가 깨집니다.
통과하도록 코드를 고치세요. **테스트를 고쳐서 통과시키지 마세요**
(라벨 구조 변경처럼 규칙 자체가 바뀌는 경우는 예외 — 그때는 단정을 갱신하고 이유를 커밋에 남깁니다).

그리고 `web/` 을 고쳤다면:

- 구문 검사 (`node --check` on 각 모듈)
- **import/export 정합성**과 **JS 가 참조하는 DOM id 가 HTML 에 존재하는지** 교차 검증
- `./serve.sh` 후 헤드리스 렌더로 **JS 에러 0건** 확인
  (`file://` 로는 ES 모듈이 CORS 로 막혀 검증이 불가능합니다)

수치를 계산하는 로직(`metrics` · `calibrate` · `infer.decide`)을 고쳤으면
`test-logic.mjs` 에 단정을 **추가**하세요. 특히 **다중 라벨 전환 시에는 손으로 계산한
라벨별 민감도·특이도·매크로 평균을 단정으로 넣으세요.** 조용히 틀린 지표를 발표하는
것이 최악입니다.

---

## 10. 배포 (Vercel)

배포는 Vercel 로 합니다. 설정은 이미 있습니다.

```bash
cd prep && python3 make_samples.py   # 내장 샘플 (업로드 실패 대비)
npx vercel --prod
```

- `vercel.json` 이 `web/` 을 정적 루트로 지정합니다. **문서·`prep/`·`data/` 는 배포되지 않습니다.**
- Vercel 은 https 이므로 secure context 요구를 만족합니다.
- **개발 중 경로 규칙**: `web/` 안에서 상대 경로만 쓰세요. `../prep/...` 같은 참조를
  런타임 코드에 넣으면 배포본에서 깨집니다.
- **모델 파일을 동봉하면 배포 용량이 커집니다.** Vercel 무료 플랜 한도를 확인하고,
  큰 모델은 외부 호스팅 + CORS 설정을 검토하세요.
- 배포 후 도움말 탭의 **환경 자동 점검**으로 모델 로드·저장소·데이터셋 표기를 확인.

Vercel MCP·플러그인은 아직 설정하지 않았습니다. `npx vercel` 로 충분하지만,
반복 배포가 많아지면 도입을 검토하세요 (필요 시 지시를 받고 진행).

---

## 11. 프론트엔드 디자인 스킬 역할 분담

디자인 관련 스킬이 두 계열 설치되어 있습니다. 담당 레이어가 다르므로 병행하되,
아래 우선순위로 판단을 나눕니다.

| 판단 대상 | 따를 계열 |
|---|---|
| 구조 · 레이아웃 · 타이포그래피 · 컬러 팔레트 · 카피 · 콘텐츠 밀도 | `taste-skill` 계열 |
| 모션 · 애니메이션 · 제스처 · 인터랙션 타이밍 · 컴포넌트 폴리시 | Emil Kowalski 계열 (`emil-design-eng`, `apple-design`, `improve-animations`) |

`taste-skill` 에도 모션 가이드가 일부 포함되어 판단이 겹칠 수 있습니다. 이때
**애니메이션 디테일(easing, spring 값, interruptible transition)은 Emil 계열을 우선**합니다.

권장 순서: 정적 레이아웃·비주얼을 먼저 완성하고, 그 위에 모션을 얹습니다.

단, `taste-skill` 은 스스로 *"dashboards / dense product UI / data tables 는 범위 밖"* 이라고
명시합니다. **콘솔 5탭(판독·성능평가·이력·설정·도움말)은 dense product UI** 이므로
랜딩 규칙(등분 카드 금지, eyebrow 상한 등)을 적용하지 마세요. 랜딩에만 적용합니다.

UX 의사결정의 상위 기준은 7장(신뢰 보정)입니다. 미학이 7장과 충돌하면 7장을 따릅니다.

---

## 12. 백로그 (우선도 낮음 — 지시가 있을 때만)

구현 완주 전에는 손대지 마세요. 규칙 1-1 이 우선입니다.

### 12-A. 모델 진단 지표 보완

TM 이 보여주는 혼동행렬과 에포크별 정확도·손실에 문제가 있다는 지적이 있었습니다.
TM 을 벗으면 **학습 쪽 진단은 팀이 쓰는 학습 환경의 문제**가 되므로, 우리 쪽에서는
홀드아웃 기반 진단을 계속 강화하는 방향이 맞습니다.

현재 보완되어 있는 것: 홀드아웃 혼동행렬, 클래스별 정밀도·재현율, 신뢰도 보정표(ECE),
임계값 스윕, 실패 사례 갤러리. 추가로 할 수 있는 것은 **회차별 홀드아웃 지표를 저장해
회차 간 비교 화면**을 만드는 것입니다.

### 12-B. 데이터셋 → 학습 → 모델 수신 자동화

TM 을 쓸 경우: **Teachable Machine 은 공개 학습 API 가 없습니다.** 브라우저 UI 전용이라
프로그래밍으로 학습을 트리거할 수 없습니다. 시도에 시간 쓰지 마세요.

TM 을 벗으면 이 제약이 사라지지만, **학습 환경은 우리 저장소 밖**입니다
(팀원의 Colab·로컬 GPU 등). 우리가 담당할 접점은 다음 두 개로 좁히세요.

1. **학습 입력 규격을 문서화**해 팀원이 그대로 쓸 수 있게 하기
   (`prep/` 산출물 구조 · 입력 크기 · 정규화 · 라벨 포맷)
2. **학습 산출물 수신 규격** — 어떤 형식(TF.js / ONNX)으로, 어떤 메타데이터
   (라벨 목록 · inputSpec · outputSpec)를 함께 달라고 요청할지 정하기

이 두 규격이 5-B 의 어댑터 계약입니다. **먼저 합의하고 학습을 시작하는 것이
나중에 변환에 시간을 쓰지 않는 방법입니다.**
