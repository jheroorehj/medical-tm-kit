# medical-tm-kit — 작업 규칙

바이오헬스AI 해커톤 산출물입니다. 이 파일은 **이 저장소에서 작업할 때 반드시 지킬 규칙**만
담습니다. 실행 절차·데모 시나리오·기획서 양식은 [`HACKATHON.md`](HACKATHON.md) 에 있습니다.

> **문서 우선순위**: 공식 배점표 > 대회 안내 사항 > 강의 정리(`docs/바이오헬스AI_해커톤정리.md`).
> 충돌하면 위쪽을 따릅니다. 강의에서 강하게 강조했지만 배점 항목이 없는 것도 있습니다.

### 팀원에게

이 파일은 **git 으로 공유되고 배포에는 포함되지 않습니다.**
`.vercelignore` 의 `*.md` 와 `vercel.json` 의 `outputDirectory: "web"` 두 겹으로 제외됩니다.
둘 중 하나를 지워도 다른 하나가 막지만, 둘 다 지우지 마세요.

**Claude Code 로 이 폴더에서 작업하면 이 파일이 자동으로 읽힙니다.** 따로 붙여넣지 않아도
아래 규칙이 적용됩니다. 사람이 읽어도 되는 문서이니 작업 시작 전에 한 번 훑어 주세요.
특히 **1장(작업 순서)** 과 **2장(절대 깨뜨리지 말 것)** 은 어기면 감점으로 직결됩니다.

처음 클론했다면:

```bash
./serve.sh                                  # http://localhost:8000, 앱의 '도움말' 탭부터
node test-logic.mjs                         # 70/70 통과해야 정상
cd prep && pip install -r requirements.txt  # 데이터 작업 담당만
```

---

## 1. 배점이 작업 순서를 정합니다 (100점)

| 배점 | 항목 | 이 저장소에서 뜻하는 것 |
|---:|---|---|
| **15** | 2.1 웹서비스 동작 | 실제로 정확히 동작할 것 |
| **15** | 2.3 모델 연동 | 실제 TM 모델로 예측이 표시될 것 |
| 10 | 1.1 데이터 이해·전처리 | `prep/` 파이프라인 |
| 10 | 1.2 학습 파이프라인 | **학습 반복과 튜닝 근거** (`HACKATHON.md` 7-A 로그) |
| 10 | 1.3 예측 성능 | 홀드아웃 수치 |
| 10 | 2.2 UI/UX | **최종 사용자(임상의) 관점** |
| 10 | 2.4 결과 시각화 | 신뢰도 표시 포함 |
| 5+5 | 3.1 발표 · 3.2 Q&A | 문제 → 해결 → 결과 |
| 5+5 | 4.1 역할분담 · 4.2 소통 | 협업 흔적 |

### 규칙 1-1. 동작이 최우선

**2.1 + 2.3 = 30점이 "실제로 동작"을 요구합니다.** 기능 추가·리팩터·디자인 개선은
전체 경로(모델 로드 → 판독 → 유사도 표시 → 히트맵 → 홀드아웃 평가)가 실제 모델로
한 번 완주한 뒤에 합니다. 완주 전에는 다른 작업을 제안하지 마세요.

### 규칙 1-2. 정확도를 숨기지 마세요

배점 1.3 이 *"프로젝트 목적에 맞는 예측 정확도 달성"* 을 직접 평가합니다.
**"정확도는 말하지 않겠습니다" 같은 문구를 산출물이나 발표 자료에 넣지 마세요.**
(이 저장소 초기 문서에 그런 문장이 있었고 정정했습니다.)

올바른 형태: 홀드아웃 정확도·민감도·특이도를 **함께** 제시하고, 측정 방법
(환자 단위 분리, TM 화면 값 아님)을 한 문장으로 밝힙니다.

### 규칙 1-3. 첫 화면은 임상의 기준으로

배점 2.2 는 최종 사용자 관점을 평가합니다. 판독 화면 첫 인상에 남기는 것은
**판정 · 유사도 · 권고 · 안전문구 · 데이터셋 출처**뿐입니다.
엔트로피 · jitter · ECE · Youden 같은 용어는 `자세히 보기` 뒤에 둡니다
(`ui.showDetailByDefault: false`). 새 전문 지표를 추가할 때도 같은 규칙을 적용하세요.

### 규칙 1-4. 보이는 것은 동작해야 합니다

배점 2.1(정확히 동작) · 2.4(완성도)에 걸립니다.
**클릭 핸들러 없는 버튼, 값이 채워지지 않는 자리, 빈 카드를 남기지 마세요.**
실제로 `#detail-toggle` 이 배선 없이 화면에 있던 사고가 한 번 있었습니다.
UI 요소를 추가하면 배선과 빈 상태(empty state)를 같이 만드세요.

---

## 2. 절대 깨뜨리지 말 것 (대회 필수 표기)

- **"확률" 금지, "유사도" 사용.** 코드·화면·문서 전부. `probability` 는 내부 변수명만.
- **판단 보류 임계값 기본 0.70.** 미달 시 단정하지 않고 `HOLD · 판단 보류` 로 표시.
- **안전 문구 상시 노출** — "교육용 프로토타입이며 실제 진단·치료에는 사용할 수 없습니다".
- **데이터셋 출처·라이선스·라벨링·클래스 구성·한계**를 화면에 표기 (`dataset` 블록).
  미기재면 화면과 도움말 환경 점검에서 경고가 뜨도록 되어 있습니다. 그 경고를 없애지 마세요.
- **업로드 실패 시 내장 샘플 자동 로드** (`failUpload()`). 데모가 끊기지 않게 하는 장치입니다.
- **입력은 이미지 업로드.** 웹캠은 `ui.enableWebcam: false` 로 꺼져 있습니다.
  실시간 판독·시간축 안정화는 웹캠 전용이므로 함께 숨겨져 있습니다. 되살리지 마세요.
- **분류 문제만.** 검출·분할은 대회 범위 밖입니다.
- **홀드아웃은 Teachable Machine 에 넣지 않습니다.** 환자(원본) 단위 분할을 유지하세요.

이 항목들은 산문이 아니라 `test-logic.mjs` 가 검사합니다 (9장 참조).

---

## 3. 데이터셋 선정 검증 (주제 확정 전 필수)

주제·데이터셋이 아직 확정되지 않았습니다. 후보가 나오면 **아래를 통과하는지 먼저
확인하고, 통과하지 못하면 후보를 바꾸라고 말하세요.** 통과 못 한 데이터셋으로
시작하면 나중에 되돌릴 시간이 없습니다.

### 3-A. 이미지 분류로 풀리는 문제인가 (가장 중요)

- [ ] **한 장의 이미지 전체를 보고 사람이 클래스를 말할 수 있는가?**
      "어디에 있는지", "몇 개인지", "경계가 어디까지인지"를 답해야 하는 문제라면
      검출·분할이며 **대회 범위 밖**입니다. 이 킷으로도 풀리지 않습니다.
- [ ] 클래스를 **2~4개**로 줄일 수 있는가? 등급형(0~4단계)이라면 병합 전략을 세울 수 있는가?
      (예: `0,1 = normal` / `2,3,4 = abnormal`. `prep/from_csv.py --map` 이 지원)
- [ ] **224×224 로 줄여도 판단 근거가 남는가?** ← TM 의 결정적 제약
      원본에서 병변이 수십 픽셀 이하면 리사이즈에서 사라집니다.
      **확인 방법**: 후보 이미지 몇 장을 `prep/pipeline.py` 로 돌려
      `data/prepared/train/` 의 224 결과물을 눈으로 보세요. 사람이 못 알아보면 모델도 못 봅니다.

### 3-B. 확보 가능성

- [ ] **가입만으로 즉시 다운로드**되는가? 승인 대기·교육 이수가 필요하면 시간이 부족합니다.
- [ ] 라이선스가 교육·비상업 사용을 허용하는가? (CC BY / CC BY-NC 등)
      재배포는 하지 않으므로 대부분 가능하지만 **반드시 확인하고 화면에 표기**합니다.
- [ ] **클래스당 100장 이상** 확보되는가? (도구 확인용은 50~100, 제출용은 100~1,000)
- [ ] 클래스 불균형이 심하지 않은가? 심하면 언더샘플링 계획이 있는가?
      (`from_csv.py --balance`, `pipeline.py` 의 `balance.equalize`)

### 3-C. 설명 가능성

- [ ] **이 데이터의 편향을 구체적으로 말할 수 있는가?**
      "단일 병원", "특정 연령대", "밝은 피부톤 위주" 처럼. 말할 수 없으면 Q&A 에서 무너집니다.
- [ ] 라벨링 방식을 아는가? (조직병리 확진 / 전문의 합의 / 판독문 자동추출)
      신뢰도가 다르고, 이것도 화면 표기 항목입니다.

찾을 곳: Kaggle · Grand Challenge · MedMNIST · Hugging Face Datasets · ISIC Archive.
피할 곳: TCIA(전처리 난이도), PhysioNet MIMIC(교육 이수 필요), Google Dataset Search(검색 품질).

---

## 4. Teachable Machine 의 성질과 한계

TM 은 **이미 학습된 MobileNet 위에 우리 클래스 분류기만 새로 학습**합니다
(전이학습). 여기서 모든 제약이 파생되므로, 성능이 안 나올 때 어디를 건드려야 하는지가
이 성질로 결정됩니다.

- **특징 추출기가 고정(freeze)되어 있습니다.** MobileNet 은 일반 사진(개·자동차·꽃)으로
  학습되어 선·모서리·색 같은 저수준 특징은 잘 잡지만, **의료영상의 미세 텍스처나
  저대비 차이는 표현하지 못할 수 있습니다.** 결과가 그럴듯해도 검증 없이 믿지 마세요.
- **모델을 바꿀 수 없습니다.** 성능 한계에 부딪히면 모델이 아니라
  **데이터 · 클래스 설계 · 전처리**로 풀어야 합니다. 그래서 `prep/` 가 성능 레버의 대부분입니다.
- **입력이 224×224 로 고정**이고 **전처리를 커스터마이즈할 수 없습니다.**
  `tmImage` 가 짧은 변을 224 로 맞추고 중앙을 정사각 크롭합니다.
  우리가 `prep/` 에서 미리 ROI 를 잡아 넣는 이유가 이것입니다.
- **브라우저에서 학습**하므로 학습 중 탭 전환 금지, 데이터 규모에 RAM 제약이 있습니다.
- 클래스명은 export 된 모델에 그대로 노출됩니다. **영문으로** 쓰고
  `project.config.js` 의 `id` 와 글자 하나까지 일치시키세요.

성능이 안 나올 때 시도 순서: **전처리(ROI·CLAHE) → 클래스 병합 → 데이터 추가 →
하이퍼파라미터.** 하이퍼파라미터를 먼저 만지는 것이 가장 효과가 적습니다.

---

## 5. 학습 → TensorFlow.js 인수 흐름

### 5-A. 데이터는 세 갈래입니다

혼동하기 쉬우니 정확히 구분하세요.

| 이름 | 위치 | 누가 쓰나 |
|---|---|---|
| **train** | `data/prepared/train/` | TM 에 업로드. TM 이 내부에서 85:15 로 나눠 15% 를 자체 검증에 씀 |
| TM 내부 validation | (TM 안에만 존재) | TM 의 화면 정확도·loss 곡선. **우리가 통제할 수 없음** |
| **holdout (= test)** | `data/prepared/holdout/` | **TM 에 절대 넣지 않음.** 우리 앱 `성능 평가` 탭에서 씀 |

**발표에 쓸 수치는 holdout 에서만 나옵니다.** TM 화면 정확도는 학습·검증 분할을
통제할 수 없어 부풀려집니다. `pipeline.py` 가 원본(환자) 단위로 분할하므로
같은 환자가 양쪽에 걸치지 않습니다 (`manifest.csv` 로 증명 가능).

### 5-B. 모델을 받아오는 두 가지 방법

TM 에서 `Export Model → Tensorflow.js → Upload my model` 을 누르면
`model.json`(구조) · `metadata.json`(클래스명) · `weights.bin`(가중치) 3개 파일이
공유 URL 에 올라갑니다. **다운로드만 하면 URL 이 동작하지 않습니다.**

1. **클라우드 URL** — 설정 탭 주분류기 칸에 붙여넣기. 브라우저에 저장되어 자동 재로드.
   정상 확인법: 새 탭에서 `<모델URL>model.json` 을 열어 텍스트가 보이면 정상.
2. **파일 동봉** — `prep/fetch_model.py <URL>` 로 `web/model/` 에 내려받고,
   URL 칸에 `./model/` 을 넣거나 `project.config.js` 의 `primary.url` 을 `'./model/'` 로 고정.
   **배포·시연에는 이 방법이 안정적입니다** (발표장 네트워크 의존이 줄어듭니다).

재학습했으면 **TM 에서 다시 Upload** 해야 URL 내용이 갱신됩니다.
회차마다 모델 URL 을 따로 보관하세요. 마지막 것이 최선이 아닐 수 있습니다.

### 5-C. 하이퍼파라미터 튜닝은 근거가 있어야 합니다

배점 1.2 가 *"충분한 학습 반복"* 과 *"적절한 클래스·데이터셋 구성"* 을 평가합니다.
**"성능이 올랐다"는 것만으로는 근거가 아닙니다.** 무엇을 보고, 무엇을 바꾸고,
어느 지표가 어떻게 변했는지를 기록해야 합니다 (`HACKATHON.md` 7-A 학습 로그).

**조정 순서** — 배치를 크게 고정 → 에포크 → **러닝레이트를 마지막에**.
러닝레이트가 가장 민감해서 먼저 만지면 다른 변수의 효과를 알 수 없습니다.

| 파라미터 | 관찰할 것 | 조정 근거 |
|---|---|---|
| Batch Size | 학습 시간 | 크게(최대 512)가 보통 유리. 데이터가 적으면 줄임 |
| Epochs | `Under the hood` 의 **loss 곡선** | train loss 는 내려가는데 **test loss 가 다시 올라가는 지점** = 과적합. 그 앞에서 멈춤 |
| Learning Rate | loss 의 수렴 양상 | 발산하면 낮춤(0.001 → 0.0001), 수렴이 너무 느리면 올림 |
| 클래스 구성 | **혼동행렬** | 특정 두 클래스가 반복해 겹치면 **병합**. 무효 입력이 안 잡히면 그 클래스 보강 |
| 전처리 | **근거 히트맵** | 근거가 주변부·글자에 몰리면 `edge_trim`·`center_crop` 조정 후 재학습 |

**TM 화면 정확도로 튜닝하지 마세요.** 그 값에 맞춰 조정하면 TM 내부 검증셋에
과적합됩니다. 판단은 **holdout 수치**로 하세요.

---

## 6. 교체 지점은 2개뿐입니다

주제가 바뀌어도 아래 두 파일만 고칩니다. 나머지 코드는 클래스 이름도 도메인도 모릅니다.

- `web/project.config.js` — 클래스 · 모델 URL · 판정 정책 · 데이터셋 · 카피 · UI 정책
- `prep/prep.config.yaml` — 경로 · 그룹 정규식 · 전처리 · 중복제거 · 균형

`decision.inferRoi` 와 `preprocess.center_crop` 은 **같은 값**이어야 합니다
(학습 전처리와 추론 전처리 일치). 실제로 한 번 어긋나 있었고(0.70 vs 0.85),
지금은 `test-logic.mjs` 가 검사합니다.

---

## 7. UX 설계 원칙 — 행동경제학 · HCI

### 7-A. 목표는 "신뢰 최대화" 가 아니라 "신뢰 보정"

사용자가 믿고 편하게 쓰게 만드는 것이 목표지만, **의료 AI 에서 신뢰를 무조건 높이는
설계는 위험합니다.** 과신은 **자동화 편향(automation bias)** 으로 이어집니다 —
사람이 AI 를 과신해 틀린 답도 그대로 따라가는 현상이고, 여기서는 환자 위험이 됩니다.

목표는 **신뢰 보정(trust calibration)** 입니다. 모델이 맞을 때는 믿게 하고,
불확실할 때는 의심하게 만드는 것. 이것이 대회의 Human-in-the-Loop 요구와도 같은 방향이고,
"편리함·쉬움" 과도 충돌하지 않습니다. **불확실할 때 불확실하다고 말해 주는 도구가
사용자에게 더 편합니다.** 나중에 뒤통수를 맞지 않기 때문입니다.

### 7-B. 적용할 것

| 원리 | 이 앱에서의 적용 |
|---|---|
| **자동화 편향 대응** | 판단 보류 · 구분 어려움 · 무효 입력을 숨기지 않고 각각 다른 색·기호로 표시 (구현됨) |
| **앵커링 회피** | 큰 숫자를 먼저 보여주면 판단이 그 값에 고정됩니다. 유사도 옆에 **권고 문구를 같은 무게로** 두세요 |
| **프레이밍 일관성** | "유사도" 하나로 통일. 같은 색은 항상 같은 의미 (빨강=이상, 초록=정상, 노랑=애매) |
| **손실 회피** | 놓침이 위양성보다 두렵다는 것이 민감도 우선의 근거입니다. 그 **이유를 화면에 적어** 임계값 선택을 납득시키세요 |
| **인지 부하 감소** | 점진적 공개(구현됨). 첫 화면 요소를 늘리지 마세요. 선택지가 늘면 판단이 느려집니다 |
| **시스템 상태 가시성** | 모델 로드 상태 · 품질 지표 · 진행률을 항상 보이게 (구현됨) |
| **오류 예방 > 오류 메시지** | 품질 게이트가 나쁜 입력을 미리 막고 촬영을 코칭합니다 (구현됨). 사후 경고보다 낫습니다 |
| **되돌리기 가능성** | 초기화 · 이력에 판정 당시 임계값 기록으로 재현 가능 (구현됨) |
| **예측 가능성** | 같은 입력 → 같은 출력. 임계값을 바꾸면 그 사실을 화면에 남기세요 |

### 7-C. 설명의 역설 — 히트맵을 다룰 때 주의

**그럴듯해 보이는 설명은 오히려 과신을 키운다**는 것이 설명가능 AI 연구의 반복된
결과입니다. 근거 히트맵을 *"모델이 옳다는 증거"* 로 제시하면 역효과입니다.

**올바른 프레이밍**: "모델이 무엇을 보았는지 검토하세요."
히트맵의 가치는 **지름길 학습을 발견하는 것**이지 판정을 정당화하는 것이 아닙니다.
`interpretMap()` 이 "근거의 68%가 주변부에 있습니다" 처럼 **의심하라는 방향**으로
문장을 만드는 이유가 이것입니다. 이 어조를 바꾸지 마세요.

### 7-D. 하지 말 것

- 신뢰를 **연출**하는 장치 — 가짜 로딩 지연, 불필요한 애니메이션으로 권위 부여,
  "AI 가 정밀 분석 중" 같은 과장 문구
- 확신을 실제보다 높게 보이게 하는 표현 — 반올림으로 100% 표시, 소수점 과다 표기
- 사용자가 끌 수 없는 자동 판정 — 항상 사람이 최종 판단한다는 구조를 유지

모션·타이밍·컴포넌트 폴리시의 구체적 값은 Emil Kowalski 계열 스킬 기준을 따릅니다
(11장, 그리고 8장의 이미 겪은 실수).

---

## 8. 이미 한 번 겪은 실수 (반복 금지)

### CSS · DOM

- `[hidden] { display: none !important; }` 를 지우지 마세요. 작성자 스타일시트의
  `display` 선언이 브라우저 기본값을 덮어써 `hidden` 이 무력화됩니다.
- **SVGElement 에는 `hidden` IDL 프로퍼티가 없습니다.** `el.hidden = true` 는 속성이
  아니라 JS 확장 프로퍼티만 만듭니다. `ui.show()` 처럼 속성을 직접 조작하세요.
- `img.src = ''` 는 문서 URL 로 해석되어 **항상 truthy** 입니다. `removeAttribute('src')`.
- 모델 클래스명은 `metadata.json` 에서 온 **외부 입력**입니다. `innerHTML` 에 넣기 전에
  `ui.esc()` 를 통과시키세요.

### 모션 (Emil Kowalski 기준)

- **`width` 애니메이션 금지.** 채우기 막대는 `transform: scaleX` + `ui.setFill(el, ratio)`.
  CSS 는 `width:100%` + `transform-origin:left` 로 맞춰져 있습니다.
- `innerHTML` 로 막대를 재생성하면 트랜지션이 재생되지 않습니다. 노드를 재사용하고
  `requestAnimationFrame` 에서 값을 넣으세요.
- **키보드로 여는 것(⌘K)에는 애니메이션을 넣지 마세요.** 반복 동작(탭 전환)은 이동 없이
  불투명도만.
- 모든 UI 전환은 **300ms 이내**. `ease-in` 은 쓰지 않습니다. 토큰
  (`--ease-out`, `--t-press/hover/state/fill`)을 쓰세요.
- 누를 수 있는 요소에는 `:active` 눌림(`scale(.94~.99)`)과 `:focus-visible` 링을 반드시.
- `prefers-reduced-motion` 에서 전체를 차단하지 마세요. **이동·확대·무한반복만** 제거하고
  색·불투명도 전환은 남깁니다.

### 데이터 파이프라인

- **중복 제거는 같은 클래스 안에서만.** 의료영상은 정상과 이상이 시각적으로 비슷해서
  클래스를 넘나들며 dHash 를 비교하면 멀쩡한 클래스가 통째로 삭제됩니다.
- YAML 의 정규식은 **작은따옴표**로. `"^(patient\d+)"` 는 YAML 이 거부합니다.
- `holdout/` 은 증강하지 않습니다.

---

## 9. 커밋 전 검증 게이트

아래를 통과하지 못하면 커밋하지 마세요.

```bash
node test-logic.mjs      # 70/70 — 판정 관문·임상 지표·캘리브레이션 + 대회 불변식 11개
for f in prep/*.py; do python3 -m py_compile "$f"; done
```

**2장의 불변식은 산문이 아니라 실행되는 테스트입니다.** `test-logic.mjs` 뒷부분이
`inferRoi == center_crop`, 임계값 0.70, 무효 클래스 존재, 클래스 id 영문, 웹캠 off,
전문지표 접힘, 안전 문구, "확률" 단정 표현 금지, 한계 hard/soft 구분을 검사합니다.
규칙을 어기면 테스트가 깨집니다. 통과하도록 코드를 고치세요.
**테스트를 고쳐서 통과시키지 마세요** (규칙 자체를 바꿔야 하는 경우가 아니라면).

그리고 `web/` 을 고쳤다면:

- 구문 검사 (`node --check` on 각 모듈)
- **import/export 정합성**과 **JS 가 참조하는 DOM id 가 HTML 에 존재하는지** 교차 검증
- `./serve.sh` 후 헤드리스 렌더로 **JS 에러 0건** 확인
  (`file://` 로는 ES 모듈이 CORS 로 막혀 검증이 불가능합니다)

수치를 계산하는 로직(`metrics` · `calibrate` · `infer.decide` · `temporal`)을 고쳤으면
`test-logic.mjs` 에 단정을 **추가**하세요. 조용히 틀린 지표를 발표하는 것이 최악입니다.

---

## 10. 배포 (Vercel)

배포는 Vercel 로 합니다. 설정은 이미 있습니다.

```bash
cd prep && python3 fetch_model.py <TM 모델 URL>   # web/model/ 에 모델 동봉 (선택, 권장)
cd prep && python3 make_samples.py                # 내장 샘플 (업로드 실패 대비)
npx vercel --prod
```

- `vercel.json` 이 `web/` 을 정적 루트로 지정합니다. **문서·`prep/`·`data/` 는 배포되지 않습니다.**
- Vercel 은 https 이므로 secure context 요구를 만족합니다 (`localhost` 와 동일하게 동작).
- **개발 중 경로 규칙**: `web/` 안에서 상대 경로만 쓰세요. `../prep/...` 같은 참조를
  런타임 코드에 넣으면 배포본에서 깨집니다.
- 배포 후 도움말 탭의 **환경 자동 점검**으로 모델 로드·저장소·데이터셋 표기를 한 번에 확인.

Vercel MCP·플러그인은 아직 설정하지 않았습니다. `npx vercel` 로 충분하지만,
반복 배포가 많아지면 도입을 검토하세요 (필요 시 지시를 받고 진행).

---

## 11. 프론트엔드 디자인 스킬 역할 분담

디자인 관련 스킬이 두 계열 설치되어 있습니다. 담당 레이어가 다르므로 병행하되,
아래 우선순위로 판단을 나눕니다.

| 판단 대상 | 따를 계열 |
|---|---|
| 구조 · 레이아웃 · 타이포그래피 · 컬러 팔레트 · 카피 · 콘텐츠 밀도 | `taste-skill` 계열 |
| 모션 · 애니메이션 · 제스처 · 인터랙션 타이밍 · 컴포넌트 폴리시 | Emil Kowalski 계열 (`emil-design-eng`, `apple-design`, `improve-animations`) |

`taste-skill` 에도 모션 가이드가 일부 포함되어 판단이 겹칠 수 있습니다. 이때
**애니메이션 디테일(easing, spring 값, interruptible transition)은 Emil 계열을 우선**합니다.

권장 순서: 정적 레이아웃·비주얼을 먼저 완성하고, 그 위에 모션을 얹습니다.

단, `taste-skill` 은 스스로 *"dashboards / dense product UI / data tables 는 범위 밖"* 이라고
명시합니다. **콘솔 5탭(판독·성능평가·이력·설정·도움말)은 dense product UI** 이므로
랜딩 규칙(등분 카드 금지, eyebrow 상한 등)을 적용하지 마세요. 랜딩에만 적용합니다.

UX 의사결정의 상위 기준은 7장(신뢰 보정)입니다. 미학이 7장과 충돌하면 7장을 따릅니다.

---

## 12. 백로그 (우선도 낮음 — 지시가 있을 때만)

구현 완주 전에는 손대지 마세요. 규칙 1-1 이 우선입니다.

### 12-A. TM 진단 지표 보완

TM 이 보여주는 혼동행렬과 에포크별 정확도·손실에 문제가 있다는 지적이 있었습니다.
**우선도 낮음 — 구현 완료 후 개선사항.**

현재 이미 상당 부분 보완되어 있습니다: 홀드아웃 혼동행렬, 클래스별 정밀도·재현율,
신뢰도 보정표(ECE), 임계값 스윕, 실패 사례 갤러리. TM 화면에 의존하지 않고
우리 수치로 판단하는 구조입니다.

추가로 할 수 있는 것: 회차별 홀드아웃 지표를 저장해 **회차 간 비교 화면**을 만드는 것.
착수 전에 "TM 의 무엇이 문제인지"를 먼저 특정하세요 (진단 UI 의 한계인지,
우리 모델의 성능 문제인지에 따라 대응이 다릅니다).

### 12-B. 데이터셋 → TM 학습 → 모델 수신 자동화

**제약을 먼저 확인하세요. 완전 자동화는 불가능합니다.**
Teachable Machine 은 **공개 학습 API 가 없습니다.** 브라우저 UI 전용이라
프로그래밍으로 데이터셋을 올리거나 학습을 트리거할 수 없습니다.
이걸 시도하는 데 시간을 쓰지 마세요.

자동화 가능한 범위는 이미 대부분 구현되어 있습니다.

| 단계 | 자동화 | 현재 |
|---|---|---|
| 데이터셋 검증·전처리 | 가능 | `pipeline.py` + `report.md` |
| TM 업로드용 폴더 준비 | 가능 | `data/prepared/train/` |
| TM 업로드·학습 | **불가 (수동)** | 도움말 탭이 단계를 안내 |
| 학습된 모델 수신 | 가능 | `fetch_model.py` |
| 앱에 적용 | 가능 | `./model/` 또는 설정 탭 |

남은 개선 여지는 **수동 구간의 안내를 매끄럽게 만드는 것**입니다
(예: 준비 완료 시 TM 링크와 클래스명 목록을 한 화면에 제시). 사용자가
"오래 걸리거나 제약이 생기면 포기해도 된다" 고 명시한 항목입니다.
