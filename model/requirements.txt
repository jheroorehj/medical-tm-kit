# Core
torch>=2.2
torchvision>=0.17
timm>=1.0.9            # ConvNeXt V2 weights from the HF Hub
huggingface_hub>=0.24
numpy>=1.24
pandas>=2.0
pillow>=10.0
scikit-learn>=1.3      # ROC-AUC / average precision for the RIADD metric

# Optional — only needed for `--formats onnx`
# onnx>=1.16
# onnxruntime>=1.18
