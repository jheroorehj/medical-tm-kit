# 대회 기준 대응 정리

[강의 정리 문서](docs/바이오헬스AI_해커톤정리.md)의 평가 기준·요구사항과 이 킷의 대응을
1:1로 맞춘 문서입니다. **대회 당일에는 이 문서만 보고 움직이면 됩니다.**

> 아래에서 `강의 3-3`, `10-2` 처럼 표기한 것은 모두 그 문서의 절 번호입니다.
> 원문이 필요하면 [docs/바이오헬스AI_해커톤정리.md](docs/바이오헬스AI_해커톤정리.md) 를 보세요.

---

## 0. 공식 배점표 대조 (100점)

심사 기준이 확정됐습니다. 아래가 배점이고, 강의에서 강조한 항목은 그 아래(1장)에 둡니다.
**충돌하면 이 배점표를 따릅니다.**

| 항목 | 배점 | 현재 상태 | 남은 일 |
|---|---:|---|---|
| **1.1** 의료데이터 이해·전처리 | 10 | 🟢 `pipeline.py` 가 ROI 크롭·CLAHE·224 정규화·여백 제거·번인 트림·중복 제거 수행. `from_csv.py` 가 CSV 라벨 데이터셋 처리 | 각 단계가 **왜** 필요한지 설명할 수 있어야 함 |
| **1.2** 학습 파이프라인 품질 | 10 | 🟡 전처리·클래스 설계·불균형 처리는 갖춤 (`report.md`, `manifest.csv`) | **학습 반복 증거가 없음** → 8장의 학습 로그를 채울 것 |
| **1.3** 예측 성능 | 10 | 🟡 홀드아웃 평가가 정확도·민감도·특이도를 산출 | **실제 모델로 수치를 뽑아야 함.** 아래 ⚠️ 참조 |
| **2.1** 웹서비스 동작 | 15 | 🔴 코드는 완성, **실제 모델로 검증 안 됨** | 최우선. 모델 하나 붙여 끝까지 돌릴 것 |
| **2.2** UI/UX (최종 사용자 관점) | 10 | 🟢 첫 화면은 판정·유사도·권고·안전문구·데이터셋 출처만. 전문 지표(불확실성 5지표·판정 5상태)는 `자세히 보기` 로 접힘 (`ui.showDetailByDefault: false`) | 그대로 유지 |
| **2.3** 모델 연동 | 15 | 🔴 레지스트리·URL 정규화·클래스 정합성 검사 구현, **미검증** | 2.1 과 동일 |
| **2.4** 결과 시각화·완성도 | 10 | 🟢 유사도 막대·히트맵·혼동행렬·곡선. **신뢰도 표시는 요구 이상**(원시 유사도 + 보정 신뢰도) | 그대로 유지 |
| **3.1** 발표 구성 (문제→해결→결과) | 5 | 🟡 데모 중심 시나리오는 있으나 **"결과" 구간이 약함** | 8장 발표 구성 참조 |
| **3.2** Q&A | 5 | 🟢 한계 문서·정직한 지표가 근거로 작동 | 예상 질문 준비 |
| **4.1** 역할 분담·협업 | 5 | ⚪ 팀 구성에 달림 | 아래 10장 |
| **4.2** 팀 커뮤니케이션 | 5 | ⚪ 팀 구성에 달림 | 아래 10장 |

### ⚠️ 앞선 조언 정정 — 정확도는 반드시 말해야 합니다

이 문서의 이전 판에는 발표 문장으로 *"정확도는 말씀드리지 않겠습니다"* 가 적혀 있었습니다.
**배점 1.3 이 "프로젝트 목적에 맞는 예측 정확도 달성"을 직접 평가하므로 그렇게 말하면 감점입니다.**

강의의 "정확도 하나로 자랑하지 말라" 와 배점의 "정확도를 달성하라" 는 충돌하지 않습니다.
올바른 태도는 이것입니다.

- **정확도를 말합니다.** 단, TM 화면의 값이 아니라 **홀드아웃으로 직접 검증한 값**으로.
- **정확도만 말하지 않습니다.** 민감도·특이도·보류율을 함께 붙여 놓침 중심으로 해석합니다.
- **측정 방법을 밝힙니다.** 환자 단위 분리, TM 에 넣지 않은 데이터라는 점을 한 문장으로.

수치를 감추는 것은 겸손이 아니라 **성능을 측정하지 못했다는 신호**로 읽힙니다
(강의 10-3 의 "실패 요인 3: 과장된 주장" 과 같은 뿌리입니다).

### 배점에 없어서 비중을 낮춘 것

강의에서 크게 강조했지만 **공식 배점에 항목이 없는 것**들입니다. 버리지는 말고, 발표
시간을 많이 쓰지 마세요.

- **한계의 정직한 명시** — 직접 배점 없음. 3.2 Q&A(5점) 에서 답변 근거로 쓰입니다.
  화면 표기는 이미 되어 있으니 발표에서는 한 슬라이드로 압축하세요.
- **"데모로 안 보이는 기능은 감점"** — 항목 없음. 다만 2.1 "실제로 정확히 동작" 과
  2.4 "완성도" 에 걸립니다. 즉 **미완성 기능이 보이는 것**이 문제입니다.
- **Vercel 배포** — 항목 없음. 2.1 안정성에 도움은 되지만 우선순위는 낮습니다.

---

## 1. 강의에서 강조한 항목 대응표 (배점 외 참고)

### 대회 측이 강조한 5가지

| # | 대회 기준 | 대응 | 상태 |
|---|---|---|---|
| 1 | **완주 > 기능 수.** 데모로 안 보이는 기능은 크게 감점 | 5장의 데모 시나리오로 8분 안에 전 기능 시연 · 샘플 내장으로 파일 없이도 동작 | ⚠️ **3장 참고** |
| 2 | **AI는 보조 도구.** 단정 표현 금지, Human in the Loop | 판정 5상태(보류·구분어려움·무효 포함) · "유사도" 표현 고정 · 권고 문구 · 안전 고지 상시 노출 | ✅ |
| 3 | **한계를 정직하게 명시.** 해결 가능/불가능 구분 | `dataset.limitations` 에 `hard`/`soft` 로 구분 표기 → 판독 화면·도움말 12번에 노출 | ✅ |
| 4 | **과장 금지.** 정확도 대신 놓침(민감도) 중심 | 민감도 우선 강조 · 임계값-민감도/특이도 트레이드오프 슬라이더 · 보정표(과신 교정) · 실패 사례 갤러리 | ✅ |
| 5 | **출처·라이선스·제한점 표기** | 판독 화면 우측 `DATASET` 카드 상시 표시 + 도움말 12번 | ✅ |

### 체크리스트 7항목 (정리 문서 30행)

| 요구 | 대응 | 상태 |
|---|---|---|
| "○○ 클래스와 유사하게 분류됨 (유사도 %)" 형태 | `decide()` 가 "…와(과) 유사도 XX%" 로 생성. "확률" 표현을 코드 어디에도 쓰지 않음 | ✅ |
| 판단 보류 임계값 기본 0.7 | `decision.holdThreshold: 0.70` · 미달 시 `HOLD · 판단 보류` + "사람이 직접 확인" | ✅ |
| 데이터셋 출처·라이선스·클래스 구성 표기 | `DATASET` 카드 (미기재 시 화면에서 경고) | ✅ |
| "교육용 프로토타입이며 실제 진단·치료에 사용 불가" 고정 | `copy.safetyNote` — 판독 화면 하단 상시 | ✅ |
| 한계 문장 표기 | `dataset.limitations` | ✅ |
| **업로드 실패 대비 fallback (내장 샘플 자동 로드)** | `failUpload()` → 내장 샘플 자동 로드 + 안내. 샘플 버튼도 노출 | ✅ |
| **Vercel 배포** | `vercel.json` · `.vercelignore` 포함 (4장 참고) | ✅ |

---

## 2. 대회 요구를 넘어서는 부분 (발표 카드)

강의가 **"할 수 없다"** 고 말한 것을 이 킷이 해결한 지점들입니다. 발표에서 이것만 짚어도 충분합니다.

| 강의에서 한 말 | 이 킷의 대응 |
|---|---|
| 6-3: *"Teachable Machine은 블랙박스라 내부를 못 열어보므로, 검증은 정성적 비교로 해야 함 — 사실상 유일한 검증 수단"* | **오클루전 히트맵으로 정량 검증.** 8×8 격자를 한 칸씩 가려 유사도 하락을 측정하고, "근거의 68%가 주변부에 있습니다 — 지름길 학습 의심"까지 문장으로 판정 |
| 6차시: *"TM은 train/test 분리를 대신해주지 않으므로 직접 폴더를 나눠야 함 (실습 시 실수 포인트)"* | `pipeline.py` 가 **원본(환자) 단위로 자동 분할.** 같은 환자가 양쪽에 걸치지 않음을 `manifest.csv` 로 증명 가능 |
| 6차시: *"confidence 높다고 정답 보증 아님"* | **신뢰도 보정표.** "모델이 90%라 말한 예측의 실제 정답률 68%" 를 구간별로 측정해 화면에 함께 표시 (ECE 수치 포함) |
| 4-3: *"모델은 '모르겠다'를 말하지 못하고 엉뚱한 이미지도 억지로 분류함"* | **무효 입력 클래스 + 게이트키퍼 2단 구조 + 4개 관문.** 고양이 사진·빈 배경을 넣으면 `INVALID` 또는 `HOLD` |
| 7-3: *"민감도와 특이도는 항상 같이 움직이므로 함께 보고해야 함"* | **임계값 슬라이더**로 재추론 없이 두 지표가 실시간 연동. `Youden 균형점` / `민감도 95% 확보점` 버튼 |
| 3-3: *"데이터 받으면 가장 먼저 클래스별 개수 세기"* | `pipeline.py` 가 `report.md` 로 자동 생성 (불균형 배수 경고 포함) |
| 10-2: *"CSV 라벨을 폴더로 정리해달라고 Claude Code에 요청"* | `from_csv.py` 로 고정 — **클래스 병합·언더샘플링·환자 그룹 키까지** 한 번에 |

---

## 3. ⚠️ 최대 리스크: 30점이 아직 미검증

배점 **2.1 웹서비스 동작(15) + 2.3 모델 연동(15) = 30점**이
*"실제로 정확히 동작한다"* 를 요구하는데, 이 킷은 **실제 TM 모델로 끝까지 돌려 본 적이
없습니다.** 코드·로직은 검증됐지만(로직 60/60, DOM 정합, 렌더 확인) 모델을 붙인
전체 경로는 미검증입니다.

**따라서 대회 당일 첫 번째로 할 일은 기능 추가가 아니라 모델 하나를 붙여 끝까지
돌려 보는 것입니다.** 아무리 거친 모델이라도 좋습니다. 이 30점이 확보되기 전에는
다른 어떤 작업도 우선순위가 아닙니다.

강의가 강조한 *"데모로 보여지지 않는 기능은 감점"* 은 공식 배점에 항목이 없지만,
2.1 "정확히 동작" 과 2.4 "완성도" 에 걸립니다. 즉 **미완성 기능이 화면에 보이는 것**이
문제입니다. 아래 시나리오로 보여줄 것만 남기세요.

### 대응: 8분 데모 시나리오 (이 순서 그대로)

```
[0:00] 랜딩 — "확신을 파는 대신 판단의 재료를 보여줍니다" 한 문장만
        → ⌘K 또는 #console/read 로 즉시 콘솔 이동            (30초)

[0:30] 정상 케이스 판독
        샘플 버튼 클릭 → 판독 → 유사도 막대 + 권고 문구        (1분)

[1:30] ★ 무효 입력 시험  ← 심사위원이 가장 먼저 볼 것
        판독 대상이 아닌 이미지 업로드 (고양이 사진 · 빈 배경 · 문서)
        → "무효 입력" 또는 "판단 보류" 가 뜨는 것을 보여줌
        → "강의에서 배운 대로 모델은 고양이 사진에도 억지로 답합니다.
           그래서 무효 입력 클래스를 따로 학습시켜 막았습니다"   (1분 30초)
        ※ 이 이미지를 미리 samples 에 넣어 두면 클릭 한 번으로 끝납니다

[3:00] ★ 근거 히트맵
        분석 시작 → 붉은 영역 확인 → 자동 해석 문장 읽기
        → "모델이 병변을 보는지 배경을 보는지 확인했습니다"    (1분 30초)

[4:30] ★ 임계값 트레이드오프
        성능 평가 탭 → 슬라이더를 좌우로 움직임
        → "민감도를 올리면 보류가 늘어납니다. 놓치면 위험한
           소견이라 민감도를 우선했고, 그 대가로 사람이 확인할
           건수가 N건 늘었습니다"                             (1분 30초)

[6:00] 보정표 + 혼동행렬
        "모델은 90%라 말하지만 실측은 68%입니다"              (1분)

[7:00] 한계 (도움말 12번 또는 판독 화면 DATASET 카드)
        해결 불가 / 개선 가능 을 구분해 읽음                   (1분)
```

**시간이 부족하면 잘라낼 순서** (뒤에서부터): 이력 탭 → 앙상블 → 게이트키퍼 → 보정표.
**절대 자르지 말 것**: 무효 입력 시험 · 근거 히트맵 · 임계값 트레이드오프.

### 보여줄 수 없는 기능은 아예 숨기기

데모에서 다루지 않을 기능은 화면에서 빼는 것이 유리합니다.

- 게이트키퍼를 안 쓸 거면 → 설정 탭에서 끈 상태로 두기 (기본 off)
- 앙상블을 안 쓸 거면 → 등록하지 않기 (기본 비어 있음)
- 랜딩을 안 쓸 거면 → `project.config.js` 의 `ui.showLandingFirst: false`

---

## 4. 배포 (Vercel)

```bash
# 1) 모델 파일을 함께 배포 — 발표장 네트워크 의존을 줄입니다 (강의 9-3)
cd prep
python3 fetch_model.py https://teachablemachine.withgoogle.com/models/<내모델>/
#   → web/model/ 에 model.json · metadata.json · weights 저장

# 2) 설정 탭 주분류기 URL 칸에  ./model/  을 넣거나
#    project.config.js 의 primary.url 을 './model/' 로 고정

# 3) 내장 샘플 생성 (업로드 실패 대비 + 파일 없이 데모)
python3 make_samples.py

# 4) 배포
cd ..
npx vercel            # 첫 실행 시 로그인 · 프로젝트 연결
npx vercel --prod
```

`vercel.json` 이 `web/` 을 정적 루트로 지정합니다. Vercel은 https라서 secure context
요구를 만족하므로 `localhost` 와 동일하게 동작합니다.

> 배포 후 반드시 확인: **모델 로드 · 샘플 판독 · 데이터셋 카드 표기.**
> 도움말 탭의 **환경 자동 점검**이 이 항목들을 한 번에 검사해 줍니다
> (안전한 주소 · 라이브러리 · 저장소 · 모델 · 클래스 정합성 · 무효 클래스 · 내장 샘플 · 데이터셋 표기).

---

## 5. 1페이지 기획서 (강의 10-2의 7항목)

그대로 채워서 제출하세요.

```
① 문제 정의 — 누구의 어떤 불편인가
   (예) ○○ 판독은 전문의가 부족한 1차 의료기관에서 대기가 길다.
        1차 선별을 보조해 "확인이 필요한 건"을 먼저 골라내고 싶다.

② 사용자 상황 — 어떤 상황에서 사용하는가
   (예) 1차 의료기관 · 검진 현장에서 촬영 직후, 전문의 판독 전 선별 단계

③ 데이터셋 — 어떤 공개 데이터셋, 어떤 클래스
   출처 / 라이선스 / 라벨링 방식 / 원본 클래스 → 사용 클래스(병합 여부) / 클래스별 장수
   → project.config.js 의 dataset 블록과 동일하게 적으세요

④ 모델 — Teachable Machine 전이학습 (MobileNet 특징 추출기 고정 + 분류기만 학습)
   Standard Image Model · 클래스 N개 · epochs/batch/LR 값 · 학습 장수

⑤ 웹 기능 — 이미지 업로드 → 클래스별 유사도 표시 (기본)
   + 판단 보류 임계값 · 근거 히트맵 · 홀드아웃 검증 · 신뢰도 보정
   ※ 데모에서 실제로 보여줄 것만 적으세요

⑥ 예상 데모 — 3장의 8분 시나리오를 요약해 적으세요

⑦ 한계 — 구체적으로. 해결 불가 / 개선 가능 을 구분
   → dataset.limitations 와 동일하게 적으세요
```

**MVP 기준으로 작성하고, 작업 중 수시로 이 기획서를 확인해 범위를 벗어나지 않는지 점검하세요.**

---

## 6. 대회 당일 시간 배분

정리 문서 기준: 총 17시간 · 실제 작업 10시간 미만 · **순수 개발·모델학습 4~6시간**.

이 킷은 이미 완성되어 있으므로 그 4~6시간을 **데이터와 검증에 쓸 수 있습니다.**

| 구간 | 할 일 | 담당 |
|---|---|---|
| ~0:30 | 주제·데이터셋 확정 (사전에 3개 골라 둘 것) | 전원 |
| 0:30~1:30 | 데이터 다운로드 → `from_csv.py` 또는 폴더 정리 → `pipeline.py` | 데이터 |
| " | 동시에: `project.config.js` 의 `dataset`·`classes` 채우기 | 웹 |
| " | 동시에: 기획서 ①②⑦ 작성 | 발표 |
| 1:30~2:30 | Teachable Machine 학습 (탭 전환 금지) → Under the hood 혼동행렬 확인 | 모델 |
| " | 동시에: `make_samples.py` 로 샘플 생성 → 샘플로 UI 먼저 테스트 | 웹 |
| 2:30~3:00 | 모델 URL 연결 → 클래스 정합성 초록 확인 → 무효 입력 시험 | 웹 |
| 3:00~4:00 | 홀드아웃 평가 → 보정표 생성 → 임계값 운영점 결정 | 모델+발표 |
| 4:00~4:30 | `fetch_model.py` → Vercel 배포 → 배포본에서 다시 확인 | 웹 |
| 4:30~5:30 | 리허설 2회 + 화면 녹화 백업 | 전원 |

### 중간 점검 기준 (강의 10-3)

> **절반 시간이 지났는데 웹에서 결과가 한 번도 떠본 적이 없다면 위험 신호.**

이 킷은 **샘플 이미지만 있으면 모델 없이도 UI가 돈다**는 점을 활용하세요.
모델이 나오기 전에 UI·발표 자료를 다 만들어 두면 병목이 사라집니다.

### 실패 3대 요인 대비

| 요인 | 대비 |
|---|---|
| **작동 안 하는 데모** | ① 내장 샘플(`make_samples.py`) ② 모델 파일 동봉(`fetch_model.py`) ③ 잘 되는 버전 백업 ④ 화면 녹화 |
| **과욕** | 3장의 "자르는 순서" 를 미리 정해 두기 |
| **과장된 주장** | 홀드아웃 수치만 발표 · TM 정확도는 언급하지 않기 · 보정표로 과신 교정 |

---

## 7. Teachable Machine 학습 설정 (강의 4-3 기준)

```
프로젝트: Image Project → Standard Image Model
클래스명: 영어로 (export된 모델에 그대로 노출됨)
          project.config.js 의 id 와 글자 하나까지 일치시킬 것

Epochs      : 50 부터. loss 그래프에서 test loss가 다시 올라가면 줄일 것 (과적합)
Batch Size  : 크게 두는 게 보통 유리 (최대 512)
Learning Rate: 마지막에 조정. 가장 민감한 값. 기본 0.001 → 흔들리면 0.0001

★ 학습 중 탭 전환 금지 (브라우저에서 돌기 때문)
★ Advanced → Under the hood 의 혼동행렬·loss 그래프를 반드시 확인
★ Export Model → Tensorflow.js → Upload my model  (다운로드만 하면 URL이 동작하지 않음)
```

**모델 URL 정상 확인법**: 새 탭에서 `<모델URL>model.json` 을 열어 텍스트가 보이면 정상.
재학습했으면 **다시 업로드**해야 URL 내용이 갱신됩니다.

---

## 7-A. 학습 로그 (배점 1.2 — 반복의 증거)

배점 1.2 는 *"충분한 학습 반복"* 과 *"적절한 클래스 라벨·데이터셋 구성"* 을 평가합니다.
**한 번 학습해서 끝내면 이 항목을 못 채웁니다.** 회차마다 아래 표를 채우세요.
표 자체가 증거이고, Q&A 에서 그대로 근거가 됩니다.

| 회차 | 클래스 구성 | 클래스당 장수 | Epochs / Batch / LR | TM 화면 정확도 | 홀드아웃 정확도 | 민감도 | 바꾼 것과 이유 |
|---:|---|---:|---|---:|---:|---:|---|
| 1 | | | 50 / 512 / 0.001 | | | | 최초 |
| 2 | | | | | | | 예: 혼동행렬에서 A↔B 가 14건 겹쳐 두 클래스를 합침 |
| 3 | | | | | | | 예: 무효 입력 클래스 추가 후 오답 유형이 바뀜 |

**회차를 만드는 판단 근거** (그대로 써도 됩니다)

- TM `Under the hood` 의 **loss 곡선**에서 test loss 가 다시 올라가면 → epochs 축소
- **혼동행렬**에서 특정 두 클래스가 반복해 겹치면 → 클래스 병합 (강의 7-2 전략)
- **아무거나 넣어도 답이 나오면** → 무효 입력 클래스 보강 후 재학습
- **근거 히트맵이 주변부를 가리키면** → 전처리에서 `edge_trim`·`center_crop` 조정 후 재학습
- 클래스별 장수가 2배 이상 벌어지면 → `augment.py --target` 또는 `--balance` 로 맞춤

> 회차마다 **TM 모델 URL 을 따로 보관**하세요. 마지막 것이 최선이 아닐 수 있고,
> 되돌릴 수 있어야 안전합니다.

---

## 8. 발표 구성 — 문제 → 해결 → 결과 (배점 3.1)

배점이 요구하는 흐름은 **문제 → 해결 → 결과** 입니다. "결과" 를 별도 구간으로 세우고
수치를 반드시 넣으세요. 이전 판은 데모에 시간을 몰아 두어 결과 구간이 약했습니다.

```
① 문제   (1분)   누구의 어떤 불편인가. 왜 분류로 풀 수 있는가.
② 해결   (1분)   데이터셋 · 클래스 설계 · TM 전이학습 · 웹 연동 한 장
③ 데모   (3분)   ★ 라이브. 정상 케이스 → 무효 입력 시험 → 근거 히트맵
④ 결과   (2분)   ★ 수치 구간. 아래 표를 슬라이드로
⑤ 한계   (1분)   해결 불가 / 개선 가능 구분해 한 장
```

### ④ 결과 슬라이드 (이 표를 그대로 채우세요)

| 지표 | 값 | 한 줄 해석 |
|---|---:|---|
| 홀드아웃 표본 | N장 | 학습에 쓰지 않은 데이터, 환자 단위 분리 |
| 정확도 | XX% | 프로젝트 목표(선별 보조) 기준 충분/부족 판단 |
| **민감도** | XX% | 놓친 이상 소견 N건 |
| 특이도 | XX% | 위양성 N건 |
| 보류율 | XX% | 사람에게 넘긴 N건 |
| 보정 오차(ECE) | 0.0XX | 모델이 말하는 확신과 실측의 격차 |

여기에 **혼동행렬 한 장**과 **가장 잘 맞은 사례 1건 + 틀린 사례 1건**을 붙이면
1.3(예측 성능)과 3.1(발표 구성)을 동시에 채웁니다.

- 배경을 길게 말하지 말고 데모와 결과를 보여주세요. 자랑하는 자리입니다.
- 진단처럼 단정하는 표현은 문구·디테일까지 지양.
- 말문이 막힐 때를 대비해 **키워드 메모**를 준비하고, 막히면 팀원에게 아이컨택으로 넘기세요.

### Q&A 예상 질문 (배점 3.2 — 정확하고 성실한 답변)

| 질문 | 답변 방향 |
|---|---|
| 정확도가 왜 이 정도인가 | 데이터 규모·클래스 난이도·224 축소 한계를 수치와 함께. 모르는 건 모른다고 |
| TM 정확도와 왜 다른가 | 학습·검증 분할을 통제할 수 없어 부풀려짐. 환자 단위로 다시 나눠 측정 |
| 이 모델을 실제로 쓸 수 있나 | 아니요. 교육용 프로토타입. 선별 보조 개념 검증까지가 범위 |
| 어디서 틀리나 | 실패 사례 갤러리를 열어 보여주기. 혼동행렬의 특정 쌍을 지목 |
| 모델이 무엇을 보고 판단했나 | 근거 히트맵 실행. 지름길 학습을 확인했다는 점 |
| 임계값은 왜 그 값인가 | 민감도 우선 근거 + 그 대가로 늘어난 확인 건수 |

### 발표에서 쓸 문장 (그대로 사용 가능)

> "홀드아웃 정확도 **XX%**, 민감도 **XX%**, 특이도 **XX%** 입니다.
> 이 수치는 Teachable Machine 화면의 값이 아니라, **환자 단위로 분리해 학습에 쓰지 않은
> 데이터**로 저희가 직접 검증한 값입니다. TM 이 보여주는 값은 학습·검증 분할을 통제할 수
> 없어 더 높게 나오는데, 그 값은 쓰지 않았습니다."

> "모델이 90%라고 말할 때 실제 정답률은 68%였습니다. 그 격차를 측정해 화면에는 보정된
> 값을 함께 보여줍니다."

> "놓치면 위험한 소견이므로 민감도를 우선해 임계값을 0.XX로 정했습니다. 그 대가로 사람이
> 확인해야 할 건수가 N건 늘었고, 그건 의도한 트레이드오프입니다."

> "이 한계는 파인튜닝으로 해결되지 않는 근본적 한계입니다. 반면 이 한계는 해당 장비
> 데이터를 확보하면 다음 단계에서 개선할 수 있습니다."

---

## 9. 웹캠은 껐습니다

이 대회의 기준 시나리오는 **이미지 업로드**입니다 (강의 8단원). 웹캠은 요구사항이 아니므로
`ui.enableWebcam: false` 로 두어 **화면에서 완전히 제거**했습니다. 함께 사라진 것:

- WEBCAM / UPLOAD 입력 선택 탭 → 업로드 화면만 남습니다
- **실시간 판독(시간축 안정화)** → 정지 이미지 1장에는 시간축이 없어 적용되지 않습니다
- 설정 탭의 이동평균 창 · 확정 조건 · 추론 간격 슬라이더
- 명령 팔레트의 카메라 명령
- 환경 자동 점검의 카메라 항목

**코드는 그대로 남아 있습니다.** `enableWebcam: true` 로 바꾸면 즉시 복귀합니다.
파이프라인은 6단계 → **5단계**(품질 검사 → 224 변환 → 게이트키퍼 → 주분류기+앙상블 → 판정)로
문서·화면 모두 정정했습니다.

> 대회 1순위 기준이 **"데모로 보여지지 않는 기능은 크게 감점"** 이므로,
> 쓰지 않을 기능을 화면에 남겨 두는 것 자체가 위험입니다.

## 9-A. 팀워크 증거 만들기 (배점 4.1 + 4.2 = 10점)

**전체의 10% 입니다.** 결과물이 좋아도 협업 흔적이 안 보이면 못 받습니다.
심사가 보는 것은 "역할이 적절히 나뉘었는가" 와 "소통이 원활했는가" 입니다.

### 역할 분담 (강의 10-2 의 4분할)

| 역할 | 담당 | 하는 일 | 산출물 |
|---|---|---|---|
| 데이터 | | 데이터셋 선정·라이선스 확인, `from_csv.py`/`pipeline.py` 실행, 클래스 확정 | `report.md`, 데이터셋 카드 |
| 모델 | | TM 학습 회차 진행, 혼동행렬 확인, 홀드아웃 평가 | 7-A 학습 로그, 모델 URL |
| 웹 | | 설정 연결, 샘플 생성, 배포, 데모 리허설 | 동작하는 앱, 배포 링크 |
| 설명 | | 문제 정의, 기획서, 발표 자료, 결과 슬라이드 | 1페이지 기획서, 발표 |

모델(TM)은 손댈 게 적으니 **웹·데이터에 인원을 더 두고**, 설명 담당이 팀 리딩을 맡는
구조가 강의 권장안입니다.

### 소통 흔적을 남기는 방법 (준비 부담 없이)

- **스탠드업 3회** — 시작 / 절반 / 리허설 전. 각자 "한 것 / 막힌 것" 한 줄씩 기록.
  막힌 사람을 빨리 찾는 것이 목적입니다. 이 기록이 4.2 의 증거가 됩니다.
- **커밋을 각자 이름으로** — 저장소 히스토리가 그대로 협업 증거입니다.
  한 사람이 전부 커밋하지 말고 담당 영역은 담당자가 커밋하세요.
- **기획서에 역할을 적어 제출** — 위 표를 그대로 옮기면 됩니다.
- 발표에서 **각자 담당 구간을 말하기** — 한 사람이 전부 발표하면 4.1 이 약해집니다.

> 혼자 참가한다면 이 10점은 구조적으로 받기 어렵습니다. 팀 구성이 가능한지
> 먼저 확인하고, 어렵다면 나머지 90점에 집중하는 전략이 맞습니다.

---

## 10. 남은 확인 사항 (배점 순)

- [ ] **[30점] 실제 TM 모델로 전체 경로 1회 완주** — 모델 로드 → 업로드 판독 →
      유사도 표시 → 히트맵 → 홀드아웃 평가. 가장 먼저, 가장 거친 모델로라도.
- [ ] **[10점] 팀 구성 확인 (대기 중)** — 9-A 의 역할 표를 채울 수 있는지
- [ ] **[10점] 학습 회차 2~3회 진행** — 7-A 학습 로그 채우기
- [ ] `project.config.js` 의 `dataset` 블록 채우기 (안 채우면 화면과 환경 점검에서 경고가 뜹니다)
- [ ] `dataset.limitations` 를 우리 데이터에 맞게 다시 쓰기 (현재는 예시 문장)
- [ ] `prep.config.yaml` 의 `dedup.enabled` 확인 (기본 true)
- [ ] 데이터셋 3개 사전 선정 — Kaggle · Grand Challenge · MedMNIST · Hugging Face
      (승인 대기·교육 이수가 필요한 것은 제외)
