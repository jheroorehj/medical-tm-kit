/* samples.js — prep/make_samples.py 가 이 파일을 덮어씁니다.
 *
 * 아직 샘플을 만들지 않은 상태입니다. 데이터가 준비되면 실행하세요:
 *     cd prep && python3 make_samples.py
 *
 * ★ 대회 필수 대비 항목입니다 (강의 10-3).
 *   업로드가 실패하면 내장 샘플이 자동으로 올라가 데모가 끊기지 않습니다.
 *   샘플이 있으면 판독 화면에 "샘플로 바로 시험" 버튼도 생깁니다.
 */
window.MTK_SAMPLES = [];
